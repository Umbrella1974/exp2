很好，这两个默认假设确认：

1. 相邻 AABB 如果面接触或有重叠区域，视为连续轨道，block 应该可以跨过去。
   但如果两个 AABB 中间存在 gap，则不能跨过去，candidate 应该被 clamp 到最后合法边界。
   第一版不需要把只在边/角接触的 AABB 当成可通行通道，除非测试里明确写出。

2. BlockMotionState.STOPPED_BY_PINCH 和 BlockMotionState.STOPPED_BY_TRACK 暂时保留，但标记为 reserved/deprecated。
   第一版核心逻辑不要实际输出这两个状态。
   主交互状态仍使用 GRABBED_PINCH_INSUFFICIENT / GRABBED_BLOCKED。
   停止原因使用 StopReason.PINCH_INSUFFICIENT / StopReason.TRACK_BLOCKED。

请进入 Stage 1.1 hardening。目标不是增加新功能，而是把核心引擎行为固定成行为契约测试。

请完成以下事项：

1. 补充 AABB track union 行为测试：
   - 两个 box 面接触时，block 可以从 box_1 进入 box_2。
   - 两个 box 有重叠区域时，block 可以从 box_1 进入 box_2。
   - 两个 box 中间有 gap 时，block 不能跨越 gap，candidate 应该被 clamp 到最后合法边界。
   - L 型或直角弯轨道中，candidate 经过连续区域时不应该卡死。
   - candidate 超出整个 track union 时，clamp_segment_to_track 返回沿 block_center -> candidate 方向的最后合法点。

2. 补充 SLIP 行为测试：
   - pinch_center 在 block box 内，pinch insufficient，但 hand_delta <= slip_motion_threshold 时，不触发 SLIP。
   - pinch_center 在 block box 内，pinch insufficient，且 hand_delta > slip_motion_threshold 时，触发 SLIP，reason = PINCH_INSUFFICIENT。
   - block 被 track blocked，但 hand_delta <= slip_motion_threshold 时，不触发 SLIP。
   - block 被 track blocked，且 hand_delta > slip_motion_threshold 时，触发 SLIP，reason = TRACK_BLOCKED。
   - 第一版不要实际输出 SlipReason.BOTH，只保留 enum/TODO。

3. 补充 tracking invalid / recovery 行为测试：
   - tracking_valid=False 时：
     - block_center 不更新
     - 不触发 contact/detach
     - 不更新 previous_pinch_center
     - PinchState = PINCH_UNKNOWN
     - StopReason = TRACKING_INVALID
   - tracking 从 False 恢复 True 的第一帧：
     - 不推动 block
     - 不发 CONTACT_ENTER
     - 不发 CONTACT_EXIT
     - 不触发 detach/release
     - 只重置 previous_pinch_center
   - 下一帧才恢复正常 contact edge event 和移动逻辑。

4. 补充 min_block_move_distance 测试：
   - candidate 非法，但 clamp 后移动距离小于 min_block_move_distance 时，block_center 不更新。
   - 仍然可以输出 blocked_info 和 StopReason.TRACK_BLOCKED。

5. 明确 blocked_info 语义：
   - clamp_segment_to_track(start, candidate, track_region) 找到 start -> candidate 线段上最后合法点 clamped_point。
   - blocked_vector = candidate - clamped_point。
   - primary_blocked_surface = blocked_vector 绝对值最大分量对应的正/负方向。
   - primary_blocked_amount = 该主方向上的绝对越界量。
   - blocked_distance = norm(blocked_vector)，这里是欧氏距离，不是归一化值。
   - all_blocked_surfaces = 所有超过 blocked_feedback_threshold 的方向。

6. 清理状态文档：
   - BlockMotionState.STOPPED_BY_PINCH / STOPPED_BY_TRACK 保留，但 docstring 标记为 reserved/deprecated。
   - 第一版核心逻辑不应输出这两个值。
   - StopReason 才是停止原因的主表达方式。

7. 不要做以下事情：
   - 不接 MANUS SDK
   - 不接 Vive Tracker
   - 不写 UI
   - 不写触觉硬件控制
   - 不把 CSV 写入核心 controller
   - 不引入真实物理碰撞或刚体模拟

完成后请运行 pytest，并报告：
- 新增了哪些测试
- 哪些行为契约被固定
- 是否有测试失败
- 是否改动了核心逻辑