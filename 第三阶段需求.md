现在进入 Stage 3，但注意：不要假设 manus_vive_com 仓库已经稳定可靠。

目标不是直接把 manus_vive_com 的代码硬接进 exp2，而是在 exp2 里建立一个稳定的 DeviceFrame adapter contract。manus_vive_com 可以后续继续修改，只要最终输出能被 parser 转换成 DeviceFrame 即可。

当前已知 manus_vive_com 的 C++ 端会发送 combined JSON，大致包含：
- timestamp
- frame
- skeletons: list
  - gloveId
  - nodes: list
    - id
    - position [x,y,z]
    - rotation [x,y,z,w]
- trackers: list
  - id
  - trackerId
  - position [x,y,z]
  - rotation [x,y,z,w]
  - quality
  - valid

但是不要假设：
- skeletons 和 trackers 一定同一时刻采样
- 每帧一定同时有 skeletons 和 trackers
- tracker 一定 valid
- frame 一定连续
- timestamp 一定是设备时间
- node 数量一定等于 25
- tracker 0 永远是目标手 tracker

请在 exp2 里新增以下模块。

1. device_frame_models.py

定义稳定的 adapter 层数据结构：

Pose3D:
- position: np.ndarray shape (3,)
- rotation: np.ndarray shape (4,) or None

ManusNodeData:
- node_id: int
- position: np.ndarray shape (3,)
- rotation: np.ndarray shape (4,) or None

ManusHandFrame:
- glove_id: str | None
- side: str | None
- nodes: dict[int, ManusNodeData]
- valid: bool

ViveTrackerFrame:
- tracker_index: int
- tracker_id: str | None
- pose_world: Pose3D
- valid: bool
- quality: int | bool | None

DeviceFrame:
- time: float
- source_timestamp: float | int | None
- source_frame_id: int | None
- tracker: ViveTrackerFrame | None
- hand: ManusHandFrame | None
- raw: dict | None

这些结构只属于 adapter 层，不要替代 Stage 1/2 的 FrameInput / ExperimentInputSample。

2. raw_manus_vive_parser.py

实现 parse_raw_manus_vive_frame(raw: dict, config) -> DeviceFrame。

要求：
- 支持 raw dict 中有 skeletons 和 trackers。
- skeletons 或 trackers 缺失时不要报错，返回对应 None / valid=False。
- 默认选择 skeleton_index=0，但参数可配置。
- 默认选择 tracker_index=0，但参数可配置。
- 如果 tracker.valid 字段为 false，则 ViveTrackerFrame.valid=False。
- 如果 tracker 缺失 position，则 tracker=None 或 valid=False。
- 如果 skeleton nodes 缺失或为空，则 hand.valid=False。
- 不要在这里判断 contact/detach。
- 不要在这里调用 BlockController。
- 不要在这里写 CSV。

3. pinch_feature_extractor.py

实现 PinchFeatureExtractor。

配置：
- thumb_tip_node_id default = 4
- index_tip_node_id default = 9

输入：
- ManusHandFrame

输出 PinchFeature:
- thumb_tip_local
- index_tip_local
- pinch_center_local
- pinch_distance
- valid

规则：
- 如果 thumb/index 节点缺失，valid=False。
- pinch_center_local = (thumb_tip + index_tip) / 2。
- pinch_distance = norm(thumb_tip - index_tip)。
- 不要做 trial 状态判断。

4. manus_vive_adapter.py

实现 ManusViveExperimentAdapter。

输入：
- DeviceFrame
- TaskCoordinateSystem
- PinchFeatureExtractor
- tracker_to_hand transform config

输出：
- ExperimentInputSample

第一版允许使用简化位置模型：
- Vive tracker position_world 作为 hand anchor。
- MANUS pinch_center_local 作为相对 tracker/hand 的局部偏移。
- pinch_center_world = tracker_position_world + configured local offset + optional scale/axis mapping TODO。
- 如果 rotation 暂时不可靠，可以先不使用 rotation，但必须在 docstring / TODO 中标明这是 Stage 3 初版，不代表最终空间融合。

重要：
- adapter 不能修改 TrialController / BlockController 状态。
- adapter 不能触发 contact/detach。
- 如果 tracker invalid、hand invalid 或 pinch invalid，则输出 tracker_valid=False 或 pinch_center_world=None，不能伪造数据。
- coordinate_space 第一版输出 "world"，交给 TrialController 用 TaskCoordinateSystem 转 task。

5. calibration_io.py

实现：
- save_task_calibration(path, task_coordinate_system, metadata=None)
- load_task_calibration(path) -> TaskCoordinateSystem

JSON 里保存：
- origin_world
- x_axis_world
- y_axis_world
- z_axis_world
- metadata

注意：
manus_vive_com C++ 端当前使用 Z-up 坐标设置，所以 calibration metadata 里要记录 up_axis_world。不要在代码里写死 Y-up。

6. run_live_preview.py

实现 live preview，不做正式实验：

流程：
- 从 ManusDataReceiver 或 raw frame source 获取最新 raw JSON。
- parse_raw_manus_vive_frame(raw) -> DeviceFrame。
- adapter.to_experiment_input_sample(device_frame)。
- TrialController.update(sample)。
- 打印：
  - tracker_valid
  - hand_valid
  - pinch_valid
  - pinch_distance
  - pinch_center_world
  - pinch_center_task
  - contact_state
  - block_motion_state
  - stop_reason
  - track_state
  - slip_active
  - blocked_force_active

不要接触觉硬件。
不要写 GUI。
不要修改 BlockController 规则。

7. tests

新增测试：

test_raw_manus_vive_parser.py:
- valid skeleton + valid tracker -> DeviceFrame valid。
- missing skeleton -> hand invalid。
- missing tracker -> tracker invalid。
- tracker valid=false -> tracker_valid false。
- node 数量不足 -> hand 可以 valid，但 pinch extractor 应返回 invalid。
- raw 中有额外字段时 parser 不崩溃。

test_pinch_feature_extractor.py:
- node 4 和 node 9 存在时正确算 center 和 distance。
- node 缺失时 valid=False。
- node id 可配置。

test_manus_vive_adapter.py:
- valid DeviceFrame -> ExperimentInputSample。
- tracker invalid -> output tracker_valid=False。
- hand invalid / pinch invalid -> 不伪造 pinch_center。
- adapter 不调用 BlockController。
- coordinate_space="world"。

test_calibration_io.py:
- 保存再加载后 world_to_task 结果一致。
- metadata 保存 up_axis_world。

test_stage3_fake_live_pipeline.py:
- 使用 fake raw JSON 序列：
  raw -> DeviceFrame -> ExperimentInputSample -> TrialController -> BlockController
- 能产生 contact_enter / block_moved / subject_end。
- 不需要真实 MANUS/Vive 硬件。

禁止事项：
- 不要把 manus_vive_com 的 receiver 内部字段名扩散到 BlockController / TrialController。
- 不要在 exp2 核心层 import manus_vive_com 的复杂校准 demo。
- 不要依赖 README。
- 不要把 offset_validation / auto_calibration / visualization 逻辑接进实验主流程。
- 不要实现复杂融合算法。
- 不要接触觉硬件。
- 不要写正式实验 UI。

完成后运行 pytest，并报告：
1. 新增文件。
2. 新增测试。
3. 是否修改 Stage 1/2 核心文件。
4. fake raw JSON 是否能跑完整 pipeline。
5. 真实 manus_vive_com 还需要哪些字段或修复。