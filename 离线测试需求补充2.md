你现在要修改 exp2 的 stage3-communication-layer 分支。

目标：
在已有 offline_replay_autocalibrated.py 的基础上，新增一个“批量离线 replay 报告工具”，用于把多组真实 raw JSONL 数据作为固定 smoke/regression cases 批量跑一遍，生成汇总表和简单预期检查。

重要背景：
当前已经有 offline_replay_autocalibrated.py，可以对单个 raw_frames.jsonl 做未正式标定数据的离线 replay：
- 自动构造临时 TaskCoordinateSystem
- 自动生成临时 block/track scene
- 跑现有 TrialController / BlockController pipeline
- 输出 frames.csv、events.csv、calibration_auto.json、scene_auto.json、summary.json

现在不要重写这个逻辑。
不要复制 offline_replay_autocalibrated.py 的核心 replay 逻辑。
不要修改：
- BlockController
- TrialController 核心逻辑
- TaskCoordinateSystem 核心逻辑
- ManusViveExperimentAdapter 核心逻辑
- raw parser / adapter / raw source 的行为语义
- offline_replay_autocalibrated.py 的核心 replay 逻辑，除非只是为了暴露一个已有 main/run 函数且保持 CLI 向后兼容

本次只新增：
1. batch_offline_replay_report.py
2. 一个 batch cases 配置示例
3. 对应测试 tests/test_batch_offline_replay_report.py

这个工具的目的：
- 一条命令批量 replay 多个 raw JSONL case
- 每个 case 调用已有 offline_replay_autocalibrated.py 的能力
- 汇总每个 case 的 summary.json
- 生成一个 batch_summary.csv / batch_summary.json
- 根据配置里的简单预期条件生成 PASS / FAIL
- 用作后续改代码前后的回归基线，防止把已经跑通的核心 pipeline 改坏

这个工具不是正式实验分析工具。
它只是 offline smoke/regression report。
不能把结果当成正式实验数据结论。

新增脚本：
batch_offline_replay_report.py

输入参数：
- --cases path，必填，指向一个 JSON 配置文件
- --out-dir 默认 data/offline_replay_batch
- --stop-on-fail 可选，如果有 case 的 expectation 失败则返回非零退出码
- --overwrite 可选，如果输出目录已存在则覆盖或清理对应 case 子目录
- --python-executable 可选，默认使用当前 Python 解释器 sys.executable

cases 配置文件格式：

{
  "cases": [
    {
      "id": "experiment_14",
      "raw_jsonl": "data/raw/experiment_14/raw_frames.jsonl",
      "description": "strong positive sample, moving should be obvious",
      "args": {
        "calibration_mode": "initial-window",
        "calibration_frames": 100,
        "scene_mode": "wide-track",
        "block_size": 0.20,
        "track_margin": 0.10,
        "track_width": 0.20,
        "z_tolerance": 0.20
      },
      "expectations": [
        {"metric": "moving_frame_count", "op": ">=", "value": 1},
        {"metric": "large_delta_frame_count", "op": "==", "value": 0}
      ]
    },
    {
      "id": "experiment_10_no_pinch",
      "raw_jsonl": "data/raw/experiment_10/raw_frames.jsonl",
      "description": "negative sample, pinch distance too large, should not move",
      "args": {
        "calibration_mode": "initial-window",
        "calibration_frames": 100,
        "scene_mode": "wide-track"
      },
      "expectations": [
        {"metric": "moving_frame_count", "op": "==", "value": 0},
        {"metric": "large_delta_frame_count", "op": "==", "value": 0}
      ]
    }
  ]
}

说明：
- cases[].id 用于输出子目录名，必须是安全文件名，只允许字母、数字、下划线、短横线、点号
- cases[].raw_jsonl 是 raw JSONL 路径
- cases[].args 会被转换成 offline_replay_autocalibrated.py 的 CLI 参数
- cases[].expectations 是简单阈值检查
- description 仅用于报告，不影响逻辑

支持的 args：
尽量覆盖 offline_replay_autocalibrated.py 的 CLI 参数：
- max_frames
- thumb_node
- index_node
- tracker_index
- skeleton_index
- up_axis
- calibration_mode
- calibration_frames
- scene_mode
- block_size
- track_margin
- track_width
- narrow_track_width
- z_tolerance

实现方式要求：

优先方式：
- 如果 offline_replay_autocalibrated.py 已经有可复用的 run/main 函数，直接 import 并调用。
- 如果目前只有 CLI，也可以用 subprocess 调用当前 Python 解释器运行 offline_replay_autocalibrated.py。
- 为了最小侵入，推荐先用 subprocess 方式，避免修改 offline_replay_autocalibrated.py。

每个 case 的输出目录：
--out-dir / case_id /

例如：
data/offline_replay_batch/experiment_14/
  frames.csv
  events.csv
  calibration_auto.json
  scene_auto.json
  summary.json

批量工具额外输出：
--out-dir / batch_summary.csv
--out-dir / batch_summary.json

batch_summary.csv 每行一个 case，至少包含：
- case_id
- description
- raw_jsonl
- status: PASS / FAIL / ERROR
- error_message
- total_raw_frames
- replayed_raw_frames
- valid_input_frames
- valid_pinch_frames
- tracker_fallback_frame_count
- invalid_input_frame_count
- moving_frame_count
- generated_contact_enter_count
- generated_contact_exit_count
- slip_active_frame_count
- blocked_frame_count
- large_delta_frame_count
- tracker_invalid_frame_count
- pinch_distance_min
- pinch_distance_mean
- pinch_distance_max
- scene_mode
- calibration_mode
- calibration_frames
- output_dir

batch_summary.json 包含：
{
  "total_cases": 0,
  "passed_cases": 0,
  "failed_cases": 0,
  "error_cases": 0,
  "cases": [
    {
      "case_id": "...",
      "description": "...",
      "raw_jsonl": "...",
      "status": "PASS",
      "summary": {...},
      "expectation_results": [
        {
          "metric": "moving_frame_count",
          "op": ">=",
          "expected": 1,
          "actual": 321,
          "passed": true
        }
      ],
      "output_dir": "..."
    }
  ]
}

expectation 支持的 op：
- "=="
- "!="
- ">"
- ">="
- "<"
- "<="
- "between"

between 格式：
{"metric": "pinch_distance_mean", "op": "between", "value": [0.01, 0.20]}

如果 metric 在 summary.json 中不存在：
- 该 expectation 失败
- 在 expectation_results 中记录 missing_metric=true

如果某个 case replay 过程报错：
- status = ERROR
- 记录 error_message
- 继续跑后续 case，除非 --stop-on-fail 被启用
- batch_summary.csv/json 仍然要生成

关于 moving_frame_count：
如果 offline_replay_autocalibrated.py 的 summary.json 目前没有 moving_frame_count，那么 batch 工具可以从该 case 的 frames.csv 中统计。
统计逻辑尽量简单：
- 优先寻找 block_motion_state 字段，统计值包含 MOVING 或 GRABBED_MOVING 的帧
- 如果没有 block_motion_state，则寻找 contact_state 字段，统计包含 MOVING 的帧
- 如果仍然没有，则 moving_frame_count = null，并在 warning 中说明无法统计
不要为了这个修改 BlockController 或 TrialController。

关于 blocked_frame_count：
优先使用 summary.json 中已有 blocked_frame_count。
如果没有，可以从 frames.csv 中统计：
- stop_reason 包含 TRACK_BLOCKED
- 或 contact_state / block_motion_state 包含 BLOCKED
不要实现新物理逻辑，只做字符串统计。

关于 warnings：
每个 case 可以收集 warnings：
- summary.json 里的 warnings
- batch 工具自己产生的 warning，例如缺 metric、无法统计 moving_frame_count 等
写入 batch_summary.json。

命令示例：
python batch_offline_replay_report.py \
  --cases data/offline_replay_batch/cases_example.json \
  --out-dir data/offline_replay_batch

配置示例：
新增 data/offline_replay_batch/cases_example.json

示例里面可以使用占位路径，不要求真实存在，但要体现几类典型 case：
- positive moving sample
- blocked sample
- no-pinch negative sample
- pinch-valid-but-outside-block negative sample
- parameter sensitivity sample

测试：
新增 tests/test_batch_offline_replay_report.py

测试要求：
- 使用 pytest tmp_path 创建 fake raw JSONL 和 fake cases config
- 不需要真实设备
- 不 import manus_vive_com
- 可以使用真实 offline_replay_autocalibrated.py 跑 fake raw JSONL
- 如果 fake raw 很难稳定触发 moving，测试不强求 moving > 0；重点测 batch runner 的行为
- 测试 batch_summary.csv 和 batch_summary.json 被生成
- 测试 PASS case
- 测试 FAIL case，例如 expectation 要求 large_delta_frame_count > 999
- 测试 ERROR case，例如 raw_jsonl 路径不存在
- 测试 metric missing 时 expectation 失败但 batch 不中断
- 测试 --stop-on-fail 会返回非零退出码或抛出 SystemExit
- 测试 case_id 文件名安全校验

禁止事项：
- 不要修改核心控制器逻辑
- 不要新增 GUI
- 不要接 socket
- 不要接 haptic 硬件
- 不要把 batch report 说成正式实验分析
- 不要在 batch 工具里实现新的 contact/slip/blocked 规则
- 不要依赖 matplotlib
- 不要依赖 manus_vive_com

文档说明：
在 batch_offline_replay_report.py 的 module docstring 里写清楚：

这个工具用于批量运行 offline_replay_autocalibrated.py，把多组未正式标定 raw JSONL 数据作为 smoke/regression cases。
它的作用是生成稳定的离线回归报告，帮助检查 parser / adapter / TrialController / BlockController pipeline 是否被后续修改破坏。
它不是正式实验数据分析工具。
每个 case 的自动 task coordinate system 和 scene 仍然来自 offline_replay_autocalibrated.py 的 post-hoc 临时自动标定。
正式实验仍然需要在线 calibration 和正式 scene 配置。
里面的数据仅做展示，实际需要填写与实际情况相符的数据进入。