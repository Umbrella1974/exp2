这几个默认假设确认，可以按这个实现：

1. 新增 DeviceAdapterConfig，不把 Stage 3 设备适配字段塞进 EngineConfig。
   DeviceAdapterConfig 放 skeleton_index、tracker_index、thumb_tip_node_id、index_tip_node_id、local_offset、local_scale、use_tracker_rotation 等字段。

2. missing / invalid 区分如下：
   - skeleton 缺失：hand=None
   - skeleton 存在但 nodes 为空：hand=ManusHandFrame(..., valid=False)
   - tracker 缺失或缺 position：tracker=None
   - tracker 存在但 valid=false：tracker=ViveTrackerFrame(..., valid=False)

3. DeviceFrame.time：
   - source_timestamp 保留 raw["timestamp"]
   - 如果 timestamp 可转 float，则 time=float(timestamp) * timestamp_scale
   - 否则 time=time.time()
   - 测试里用固定 timestamp 保证可预测
   - 不要假设 source_timestamp 一定是真正设备采样时间

4. side 解析：
   - 尝试读取 side / handedness / hand
   - 都没有则 side=None
   - 不要强行从 gloveId 推断左右手

5. adapter 输出 ExperimentInputSample 时采用严格策略：
   - tracker invalid、hand invalid、pinch invalid 任意一个发生，都输出 tracker_valid=False 且 pinch_center_world=None
   - Stage 3 第一版把 ExperimentInputSample.tracker_valid 当作“本帧整体输入是否可用于驱动物块”的总开关
   - 不伪造可用 pinch_center

6. run_live_preview.py 不硬依赖 manus_vive_com。
   - 第一版支持 fake raw JSON / JSONL source
   - receiver source 只留清晰接口和 TODO
   - pytest 不依赖真实设备或外部 repo

7. subject_end：
   - adapter 可从 raw.get("subject_end", False) 透传到 ExperimentInputSample
   - 真实实验里 subject_end 也可以由外层输入源注入