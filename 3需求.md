现在接入现有 manus_data_receiver.py。
要求：
1. 不修改核心 block_controller 的逻辑。
2. 新增 adapter，把 receiver 输出转换成 FrameInput。
3. Vive Tracker 提供 hand / wrist 的世界位置。
4. Manus 提供 thumb tip、index tip 或 finger joint 数据。
5. 先实现 pinch_center_task 的占位接口：
   - 如果已有 thumb/index tip，则 pinch_center = mean(thumb_tip, index_tip)
   - 如果还没有，则允许 mock 数据输入
6. world_to_task 坐标转换独立放在 TaskCoordinateSystem。
7. 不把 SDK 字段名扩散到 block_controller 内部。