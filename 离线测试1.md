你现在要修改 exp2 的 stage3-communication-layer 分支。

目标：
新增一个离线测试工具，用于在“没有正式任务坐标系标定”的真实 raw JSONL 数据上测试 exp2 pipeline。

重要背景：
当前真实数据来自 manus_vive_com 保存的 raw_frames.jsonl。
这批数据没有经过正式 task calibration：
- 没有受试者选择原点
- 没有受试者定义 x 轴方向
- 因此不能直接用写死的 block/track 场景产生合理交互

本阶段不要修改核心逻辑。
不要修改：
- BlockController
- TrialController 核心逻辑
- TaskCoordinateSystem 核心逻辑
- ManusViveExperimentAdapter 核心逻辑
- 现有 parser / adapter / raw source 的行为语义

尽量不要修改已有文件内容。
优先通过新增脚本和新增测试完成。
如果确实需要少量 import 或 packaging 相关调整，必须保持向后兼容，并在说明中解释原因。

只新增离线 replay / diagnostic 工具。
不要接真实 socket。
不要接触觉硬件。
不要写 GUI。
不要实现新的物理规则。
不要把自动标定结果当成正式实验标定。

新增脚本：
offline_replay_autocalibrated.py

功能：
读取真实 raw JSONL，自动构造一个临时 task coordinate system，并自动摆放 block 和 track，用于离线检查真实数据能否驱动现有 exp2 pipeline。

这个工具只做 post-hoc offline smoke test。
它的作用是：
- 检查真实 raw JSONL 是否能经过 parser / adapter / TrialController
- 检查 pinch / contact / slip / blocked / large_delta 等事件是否能被现有 pipeline 生成
- 检查未正式标定数据在一个临时 task space 里是否能基本 replay

它不是正式实验分析工具。
它生成的 task coordinate system 和 scene 都是临时自动构造的。
正式实验仍然需要在线 calibration。

输入参数：
- --raw-jsonl path，必填
- --max-frames 可选，用于限制 replay 总帧数
- --thumb-node 默认 4
- --index-node 默认 9
- --tracker-index 默认 0
- --skeleton-index 默认 0
- --up-axis 默认 "0,0,1"
- --calibration-mode，可选：
    initial-window
    pca
  默认 initial-window
- --calibration-frames 默认 100
  含义：只使用前 N 个有效输入点来估计临时 task coordinate system 的 origin 和 x 方向。
  注意：
  - 这里的“有效输入点”是经过 parser + adapter 后得到的有效 pinch_center_world 或 tracker fallback 点
  - 如果 raw frame 很多，但前面有无效帧，应跳过无效帧，直到收集到最多 N 个有效点
  - calibration 只用这些点；后续 replay 仍然使用所有帧或 --max-frames 限制内的所有帧
- --scene-mode，可选：
    wide-track
    fitted-corridor
    narrow-corridor
  默认 wide-track
- --block-size 默认 0.20，单位 m
- --track-margin 默认 0.10，单位 m
- --track-width 默认 0.20，单位 m
- --narrow-track-width 默认 0.08，单位 m
- --z-tolerance 默认 0.20，单位 m
- --out-dir 默认 data/offline_replay

核心流程：

1. 读取 raw JSONL

使用 JsonlRawFrameSource。
每帧调用 parse_raw_manus_vive_frame。
每帧使用 ManusViveExperimentAdapter 得到 ExperimentInputSample。

不要修改 ManusViveExperimentAdapter 的融合逻辑。
如果当前 adapter 没有使用 tracker rotation，本工具也不要补一个新的融合模型。
本工具只用于诊断现有 pipeline 在真实数据上的表现。

2. 提取有效轨迹点

对每帧得到的 ExperimentInputSample：
- 优先使用 sample.pinch_center_world
- 如果 pinch_center_world 无效，可以使用 tracker position world 作为 fallback
- 只使用 tracker_valid=True 的帧
- 记录该点来源：
    pinch
    tracker_fallback

注意：
- 有效轨迹点用于两件事：
  1. 前 calibration_frames 个有效点用于自动构造临时 TaskCoordinateSystem
  2. 所有有效点用于计算 task trajectory range 和生成 scene
- 不要只用第一帧和最后一帧估计方向

3. 自动构造 TaskCoordinateSystem

先收集 calibration_points：
- 从有效轨迹点开头开始取
- 最多取 --calibration-frames 个点
- 如果有效点少于 --calibration-frames，也允许继续运行，但要在 summary.json 里记录 calibration_points_count
- 如果有效点少于 2 个，应报错退出，并给出清楚错误信息

如果 calibration_mode = initial-window：
- origin_world = calibration_points[0]
- x_point_world 的选择：
  - 优先使用 calibration_points 中距离 origin_world 最远的点
  - 如果最远点与 origin_world 的距离太短，例如小于 1e-4 m，则 fallback 到全部有效点中距离 origin_world 最远的点
  - 如果仍然太短，则报错退出，提示无法从数据中估计 x 方向
- 这个模式不要使用整段轨迹的最后一点作为 x_point_world

如果 calibration_mode = pca：
- 默认只对 calibration_points 做 PCA，而不是对整段轨迹做 PCA
- origin_world = calibration_points[0] 或 calibration_points 的均值，优先使用 calibration_points[0] 以便 task 原点接近动作起点
- x_axis_world = calibration_points 的第一主成分方向
- 为了方向一致性：
  - 计算 calibration_points[-1] - calibration_points[0]
  - 如果 x_axis_world 与这个向量点积为负，则翻转 x_axis_world
- x_point_world = origin_world + x_axis_world
- 如果 calibration_points 在空间中过于集中，导致 PCA 不稳定，应 fallback 到 initial-window 的最远点方法

up_axis_world 来自 --up-axis。
使用已有 TaskCoordinateSystem.build_from_origin_and_x_point。
不要修改 TaskCoordinateSystem 本身。

在 calibration_auto.json 中保存：
- calibration_mode
- calibration_frames
- calibration_points_count
- origin_world
- x_point_world
- up_axis_world
- x_axis_estimation_method
- x_axis_estimation_distance
- whether_fallback_used
- warning messages，如果有

4. 转换轨迹到 task 坐标

使用自动生成的 TaskCoordinateSystem，把所有有效轨迹点转换成 task 坐标。

输出轨迹范围：
- x_min / x_max
- y_min / y_max
- z_min / z_max
- median_z
- point_count
- pinch_point_count
- tracker_fallback_point_count

5. 自动生成测试场景

根据所有有效点的 task 坐标范围生成临时 scene。

scene_mode = wide-track：
- block_center = 第一帧有效 task 点
- track 是一个很大的 AABB，覆盖全部轨迹范围并加 track_margin
- y / z 方向也覆盖轨迹范围并加 margin
- 目的：测试 contact / pinch / slip / pipeline，不重点测试 blocked

scene_mode = fitted-corridor：
- block_center = 第一帧有效 task 点
- track 是沿 task x 方向的 corridor：
  - x 覆盖轨迹 x 范围并加 track_margin
  - y = [-track_width/2, +track_width/2]
  - z = [median_z - z_tolerance, median_z + z_tolerance]
- 目的：测试沿主运动方向推动

scene_mode = narrow-corridor：
- 与 fitted-corridor 类似
- 但 y 宽度使用 --narrow-track-width，而不是 --track-width
- 目的：故意更容易触发 GRABBED_BLOCKED / STOP_REASON.TRACK_BLOCKED

在 scene_auto.json 中保存：
- scene_mode
- block_size
- block_center_task
- track bounds
- track_margin
- track_width 或 narrow_track_width
- z_tolerance
- task_trajectory_range
- warning messages，如果有

6. 创建 TrialController

用自动生成的 TaskCoordinateSystem 和 track/block 配置创建 TrialController。

要求：
- 使用现有 block_controller_factory
- trial_id = "offline_autocalibrated"
- start_trial(time=第一帧有效 sample time, trial_id=...)
- 不修改 TrialController 核心逻辑
- 不修改 BlockController 核心逻辑
- 不修改 TaskCoordinateSystem 核心逻辑

7. replay 所有帧

replay 输入范围：
- 默认 replay raw JSONL 中所有帧
- 如果提供 --max-frames，则最多 replay 前 max_frames 个 raw frames

每帧：
- raw frame -> parse_raw_manus_vive_frame
- DeviceFrame -> ManusViveExperimentAdapter -> ExperimentInputSample
- 使用自动 TaskCoordinateSystem 转 task
- 调用 TrialController.update
- 收集：
    - frame index
    - sample time
    - tracker_valid
    - hand_valid
    - pinch_valid
    - pinch_distance
    - input point source: pinch / tracker_fallback / invalid
    - pinch_center_world
    - pinch_center_task
    - block center task
    - contact state
    - stop reason
    - haptic feedback state
    - generated events

注意：
- 不要在 replay 中实现新的接触规则
- 不要手动制造 contact/slip/blocked
- 一切事件都必须来自现有 TrialController / BlockController 输出

8. 输出结果

在 --out-dir 下保存：
- frames.csv
- events.csv
- calibration_auto.json
- scene_auto.json
- summary.json

summary.json 至少包含：
- total_raw_frames
- replayed_raw_frames
- valid_input_frames
- valid_pinch_frames
- tracker_fallback_frame_count
- invalid_input_frame_count
- calibration_points_count
- generated_contact_enter_count
- generated_contact_exit_count
- slip_active_frame_count
- blocked_frame_count
- large_delta_frame_count
- tracker_invalid_frame_count
- pinch_distance_min
- pinch_distance_mean
- pinch_distance_max
- task_trajectory_range
- scene_mode
- calibration_mode
- calibration_frames
- warnings

9. 可选生成图像

如果 matplotlib 可用，生成：
- task_trajectory_xyz.png
- pinch_distance_over_time.png
- block_center_xyz_over_time.png
- contact_state_over_time.png
- stop_reason_counts.png

图像生成失败不应影响主流程。
如果 matplotlib 不存在或绘图失败，只在 summary.json 的 warnings 中记录，不要让脚本失败。

10. 测试

新增：
tests/test_offline_replay_autocalibrated.py

使用 fake raw JSONL 测试，不需要真实设备数据。
测试不得 import manus_vive_com。

测试至少覆盖：
- initial-window calibration 能生成 TaskCoordinateSystem
- --calibration-frames 会影响用于估计 x 方向的点数
- initial-window 模式不会使用整段轨迹最后一点作为默认 x 方向
- pca 模式默认只使用前 calibration_frames 个有效点
- wide-track 模式能跑完整 replay
- fitted-corridor 模式能生成 corridor track
- narrow-corridor 模式使用 narrow_track_width
- 输出 summary 包含 contact / slip / blocked / large_delta / tracker_invalid 等统计字段
- 输出 calibration_auto.json 包含 calibration_points_count、origin_world、x_point_world
- 输出 scene_auto.json 包含 block_center_task 和 track bounds

实现建议：
- 为了可测试性，可以把脚本中的主要逻辑拆成纯函数，例如：
    - load_samples_from_raw_jsonl(...)
    - collect_valid_trajectory_points(...)
    - build_autocalibrated_task_coordinate_system(...)
    - build_auto_scene(...)
    - run_offline_replay(...)
    - write_outputs(...)
- 但不要为了拆函数修改已有核心模块
- 新函数可以放在 offline_replay_autocalibrated.py 内部，避免影响现有项目结构

文档说明：
在 offline_replay_autocalibrated.py 的 module docstring 里明确写：

这个工具只用于“未正式标定数据”的离线 smoke test。
它生成的是 post-hoc 临时 task coordinate system 和测试场景。
自动估计的 task x 轴默认来自前 calibration_frames 个有效点，而不是正式实验中的受试者标定。
不能把结果当作正式实验数据分析结果。
正式实验仍然需要在线 calibration。