这 5 点都同意，按你的方案实现。另请补充以下记录字段：

1. tracker fallback 只在 offline_replay_autocalibrated.py 中实现，不改 adapter；tracker_fallback_frame_count 单独统计，不计入 valid_pinch_frames。

2. replay 时强制 subject_end=False 可以，但需要在 summary.json 中记录：
   - raw_subject_end_frame_count
   - forced_subject_end_false = true

3. 使用超大 trial_timeout_seconds 可以，但需要在 summary.json 中记录：
   - offline_trial_timeout_seconds
   - timeout_effectively_disabled_for_offline_replay = true

4. fitted-corridor / narrow-corridor 的 y 中心继续使用 0，但需要在 scene_auto.json 或 summary.json 中记录：
   - task_y_min
   - task_y_max
   - task_y_median
   - corridor_y_center = 0.0
   如果 abs(task_y_median) > track_width / 2，加入 warning。

5. matplotlib 图像保持 best-effort，失败只写 warning，不影响 frames.csv、events.csv、calibration_auto.json、scene_auto.json、summary.json 输出。

此外，请继续遵守：不修改 BlockController / TrialController / TaskCoordinateSystem / ManusViveExperimentAdapter 核心逻辑；本工具只新增离线 replay / diagnostic 能力。

最关键的是：
凡是离线工具为了“跑完整数据”而偏离正式实验行为的地方，都要写进 summary。或者记录在显眼的地方。