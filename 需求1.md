你的 6 个问题都很有价值，我按下面规则拍板：

1. tracker_valid=False 时：
   - 不更新 block_center
   - 不触发 contact/detach
   - 不更新 previous_pinch_center
   - PinchState = PINCH_UNKNOWN
   - 新增 StopReason.TRACKING_INVALID 或 tracking_valid 字段
   - TrackState 不负责表达 tracking invalid，因为 TrackState 只描述轨道约束
   - tracking 恢复 valid 的第一帧只重置 previous_pinch_center，不推动木块

2. 主状态和停止原因分开：
   - BlockMotionState 表示主交互状态，例如 GRABBED_PINCH_INSUFFICIENT、GRABBED_BLOCKED、GRABBED_MOVING
   - StopReason 表示为什么没有正常移动，例如 PINCH_INSUFFICIENT、TRACK_BLOCKED、LARGE_DELTA、TRACKING_INVALID
   - 触觉反馈主要读 StopReason 和 HapticFeedbackState

3. blocked 信息定义为：
   - clamp_segment_to_track(start, candidate, track_region) 找到 start -> candidate 线段上的最后合法点 clamped_point
   - blocked_vector = candidate - clamped_point
   - primary_blocked_surface = blocked_vector 绝对值最大分量对应的正/负方向
   - primary_blocked_amount = 该主方向上的绝对越界量
   - blocked_distance = norm(blocked_vector)，这里 norm 是欧氏范数，不是归一化值
   - all_blocked_surfaces = 所有超过 blocked_feedback_threshold 的方向

4. reference_box 第一版不使用。
   更推荐让 clamp_segment_to_track 返回 ClampResult，其中包含 clamped_point 和 blocked_info。
   reference_box 可以作为未来 active segment / surface 判定扩展，不参与第一版计算。

5. CSV 记录不要写进核心引擎。
   核心引擎只输出 FrameOutput、Event list、HapticFeedbackState。
   独立 Logger 负责写 frames.csv 和 events.csv。

6. max_detach_count 和 trial_timeout_seconds 不属于第一阶段核心木块控制器。
   第一阶段只维护 detach 计数。
   第二阶段 TrialController 再根据 max_detach_count 和 trial_timeout_seconds 判断 FAILED_TOO_MANY_DETACHES / FAILED_TIMEOUT。