我建议补的 3 个小点

1. TrialState.TRACKING_INVALID 最好标注 reserved / future use。
现在 enum 里仍然有 TRACKING_INVALID，但第一版逻辑不实际进入这个状态。这个没问题，但建议在 docstring 或注释里写清楚：Stage 2 当前 tracking invalid 期间保持 RUNNING，TRACKING_INVALID 只是预留。

2. coordinate_space 最好校验非法值。
现在 _pinch_center_task() 里只有 coordinate_space == "world" 时走 world 转换，其他情况默认当 task。建议明确只允许 "world" 和 "task"；如果传入别的字符串，抛 ValueError。这能防止以后 adapter 写错字段名时静默当 task 处理。

3. reset() 里的 trial_reset 时间现在是 0.0，有点怪。
这不是大 bug，但如果后面真的用 reset 事件，最好改成 reset(time=None)，或者第一版 reset 不写事件。否则 events.csv 里可能出现一个伪造的 time=0.0。