现在第一阶段核心逻辑已经完成。请先不要接 MANUS/Vive，也不要写 UI。请做一轮 stage 1.1 hardening：

1. 补充多 AABB track union 测试：
   - 两个相邻/重叠 box，block 可以从第一个 box 移动到第二个 box。
   - L 型或直角弯轨道中，candidate 通过重叠区域时不应卡死。
   - candidate 超出整个 union 时，clamp 到沿 hand_delta 方向最后合法点。

2. 补充 SLIP 负例测试：
   - pinch insufficient 但 hand_delta <= slip_motion_threshold 时，slip_active=False。
   - track blocked 但 hand_delta <= slip_motion_threshold 时，slip_active=False。
   - 只有 hand_delta > slip_motion_threshold 且 PINCH_INSUFFICIENT 或 TRACK_BLOCKED 时才触发 SLIP。

3. 补充 tracking invalid 边界测试：
   - 先 inside block，然后 tracker_valid=False。
   - invalid 期间输入位置变到 outside。
   - 恢复 valid 的第一帧不发 CONTACT_EXIT，不计 detach，不移动 block，只重置 reference。
   - 下一帧才恢复正常逻辑。

4. 补充 min_block_move_distance 测试：
   - candidate 非法，但 clamp 后移动距离小于 min_block_move_distance 时，block_center 不更新。
   - 仍然可以输出 blocked_info / stop_reason=TRACK_BLOCKED。

5. 清理或注释 BlockMotionState 中 STOPPED_BY_PINCH / STOPPED_BY_TRACK：
   - 如果保留，标注为 reserved/deprecated。
   - 第一阶段逻辑应优先使用 StopReason 表达停止原因。

6. 不改变核心行为，不接 SDK，不接 UI，不接硬件。