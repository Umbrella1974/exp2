请先做 Stage 1.1 hardening，不要进入 Stage 2，不要接 SDK/UI/设备。

目标：不改变核心行为，只补充行为契约测试和少量注释。

请补充测试：

1. AABB track union：
- 两个 box 面接触时，block 可以跨过去。
- 两个 box 有重叠时，block 可以跨过去。
- 两个 box 中间有 gap 时，block 不能跨过去，应 clamp 到最后合法边界。
- L 型 / 直角弯轨道中，candidate 经过连续区域时不应卡死。

2. SLIP 负例：
- pinch insufficient 但 hand_delta <= slip_motion_threshold 时，不触发 SLIP。
- track blocked 但 hand_delta <= slip_motion_threshold 时，不触发 SLIP。
- SlipReason.BOTH 第一版保留但不实际输出。

3. Tracking invalid 边界：
- 先 inside block。
- 然后 tracker_valid=False。
- invalid 期间输入位置变到 outside。
- 恢复 valid 第一帧不发 CONTACT_EXIT，不计 detach，不移动 block，只重置 reference。
- 下一帧才恢复正常逻辑。

4. BlockMotionState.STOPPED_BY_PINCH / STOPPED_BY_TRACK：
- 保留 enum。
- 在 docstring 里标注 reserved/deprecated。
- 第一版核心逻辑不应输出它们。
- 停止原因使用 StopReason。

完成后运行 pytest，并报告是否全部通过。