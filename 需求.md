你是一个资深 Python 工程师。请帮我实现一个“受轨道约束的虚拟木块交互核心引擎”。现在不要接入 MANUS SDK、Vive Tracker、socket、UI 或触觉硬件。第一阶段只写纯 Python 核心逻辑和单元测试。

背景：
我有一个实验任务。受试者用拇指和食指的 pinch center 控制一个虚拟木块。木块中心必须在一条有宽度的 3D 轨道区域内移动。轨道第一版用多个 axis-aligned 3D boxes 的并集表示。pinch center 进入木块 box 后，木块隐藏；只有脱手后木块才显示。pinch distance 决定是否能驱动木块。木块不吸附到 pinch center，而是使用 pinch center 的逐帧 delta 推动。

请实现以下模块，保持代码清晰、可测试、可维护：

1. data_models.py
定义 dataclass / Enum：
- Vec3 可以使用 numpy.ndarray 或简单 tuple，但要统一。
- Box3D: center, size 或 min/max。
- TrackRegion: 由多个 Box3D 组成。
- BlockState / FeedbackState / FrameInput / FrameOutput。
- ContactState:
  - OUTSIDE_BLOCK
  - INSIDE_BLOCK
- PinchState:
  - PINCH_VALID
  - PINCH_INSUFFICIENT
  - PINCH_UNKNOWN
- BlockMotionState:
  - FREE_VISIBLE
  - CONTACT_HIDDEN
  - GRABBED_MOVING
  - GRABBED_BLOCKED
  - GRABBED_PINCH_INSUFFICIENT
  - STOPPED_BY_PINCH
  - STOPPED_BY_TRACK
  - STOPPED_BY_LARGE_DELTA
- TrackState:
  - INSIDE_TRACK
  - BLOCKED_X_POS
  - BLOCKED_X_NEG
  - BLOCKED_Y_POS
  - BLOCKED_Y_NEG
  - BLOCKED_Z_POS
  - BLOCKED_Z_NEG
  - HAND_DELTA_TOO_LARGE
- DetachState:
  - NONE
  - ACTIVE_RELEASE
  - FORCED_DETACH
  - UNEXPECTED_DETACH

2. geometry.py
实现纯几何函数：
- point_in_box(point, box, epsilon=0.0)
- point_in_track(point, track_region, epsilon=0.0)
- blocked_info_for_point(point, track_region, reference_box=None)
  返回：
    primary_blocked_surface
    primary_blocked_amount
    blocked_distance_norm
    blocked_vector
    all_blocked_surfaces
- clamp_segment_to_track(start, end, track_region)
  说明：
    start 是当前真实 block_center，应该在轨道内。
    end 是 candidate block_center。
    如果 end 在轨道内，返回 end。
    如果 end 不在轨道内，沿 start -> end 线段找到最后一个仍在整个 track union 内的点。
    可以用二分搜索实现，不要写复杂解析几何。
    这个函数基于整个 track union，而不是单个 active segment。

3. pinch_model.py
实现 pinch hysteresis：
- 使用两个阈值：
  - pinch_grab_threshold
  - pinch_release_threshold
- 当 distance < grab_threshold -> PINCH_VALID
- 当 distance > release_threshold -> PINCH_INSUFFICIENT
- 中间区间保持上一帧 pinch state
- 如果数据无效，返回 PINCH_UNKNOWN

4. block_controller.py
实现核心状态更新类 BlockController。

每一帧输入 FrameInput：
- time
- pinch_center_task
- pinch_distance
- tracker_valid
- subject_end 可先预留
- current block_center 内部保存
- previous_pinch_center 内部保存
- previous states 内部保存

规则：
A. 木块真实位置只由 block_center 表示。candidate 只是预测位置。
B. contact / detach 判断只看当前真实 block_center 对应的 block box，不看 candidate。
C. pinch_center 进入 block box 后，木块立刻隐藏。
D. pinch distance 不足时：
   - 木块隐藏
   - 木块不移动
   - 状态为 GRABBED_PINCH_INSUFFICIENT 或 STOPPED_BY_PINCH
   - 不计 release
   - 更新 previous_pinch_center，避免之后重新捏紧时跳变
E. pinch distance 恢复有效时：
   - 不重新显示
   - 不重新抓取
   - 直接恢复控制
   - 恢复那一帧不要把之前累计的位移一次性作用到木块上
F. 使用逐帧 delta 推动物块：
   hand_delta = current_pinch_center - previous_pinch_center
   candidate = block_center + hand_delta
G. 如果 hand_delta 超过 max_hand_delta_per_frame：
   - 木块不移动
   - 状态为 HAND_DELTA_TOO_LARGE / STOPPED_BY_LARGE_DELTA
   - 更新 previous_pinch_center
H. 如果 candidate 在 track union 内：
   - block_center = candidate
   - 状态为 GRABBED_MOVING
I. 如果 candidate 不在 track union 内：
   - 使用 clamp_segment_to_track(block_center, candidate, track_region)
   - block_center 移动到最后合法点
   - 如果移动距离小于 min_block_move_distance，可视为没移动
   - 状态为 GRABBED_BLOCKED / STOPPED_BY_TRACK
   - 输出 primary_blocked_surface、primary_blocked_amount、blocked_distance_norm、blocked_vector、all_blocked_surfaces
   - blocked feedback 需要 blocked_feedback_threshold，低于阈值时可以记录但不触发强反馈
J. 每帧都更新 previous_pinch_center，除非当前 pinch_center 无效。
K. pinch_center 离开当前真实 block box 时，木块显示，并根据上一状态分类 detach：
   - 上一状态是 GRABBED_PINCH_INSUFFICIENT / STOPPED_BY_PINCH -> ACTIVE_RELEASE
   - 上一状态是 GRABBED_BLOCKED / STOPPED_BY_TRACK -> FORCED_DETACH
   - 上一状态是 GRABBED_MOVING 且 pinch valid -> UNEXPECTED_DETACH
L. detach 计数必须边缘触发：
   - 只在 inside_block -> outside_block 的瞬间计数
   - 不允许手在 box 外时每帧重复计数
M. 分开记录：
   - active_release_count
   - forced_detach_count
   - unexpected_detach_count
   - total_detach_count

5. config.py
所有阈值都放在 config，不要写魔法数字：
- block_size_x/y/z
- track_epsilon
- pinch_grab_threshold
- pinch_release_threshold
- max_hand_delta_per_frame
- min_block_move_distance
- blocked_feedback_threshold
- binary_search_iterations
- max_detach_count
- trial_timeout_seconds

6. tests/
请使用 pytest 写单元测试，至少覆盖：
- point_in_box
- point_in_track with multiple boxes
- clamp_segment_to_track: candidate inside
- clamp_segment_to_track: candidate outside, returns boundary point
- pinch hysteresis
- pinch center enters box -> block hidden
- pinch insufficient -> block hidden but not moving
- pinch valid -> block moves by hand_delta
- candidate outside track -> block moves to last legal boundary point, not full candidate
- blocked info records primary and all blocked surfaces
- blocked then hand moves back -> can resume GRABBED_MOVING
- large hand delta -> STOPPED_BY_LARGE_DELTA, block does not move
- inside -> outside edge triggers detach count once
- forced detach vs active release vs unexpected detach classification

工程要求：
- 不要写 SDK 接入。
- 不要写 UI。
- 不要写触觉硬件控制。
- 不要把所有逻辑塞进一个 update 函数。
- geometry、pinch_model、block_controller 要分开。
- 每个 public function 和 class 写简短 docstring。
- 优先写清晰、可测、可扩展的代码。
- 如果某些规则有歧义，请在代码 TODO 中标注，不要自行发明复杂物理模型。



请先认真阅读下面的领域语义说明。不要只根据 enum 名字猜含义。

这个程序不是物理引擎，也不是真实碰撞模拟。它是一个实验任务里的“规则驱动交互引擎”。

核心对象有三个：

1. pinch_center
   - 表示受试者拇指和食指之间的捏合中心点。
   - 这是手的输入点。
   - 它每一帧都可能移动。
   - 它不等于木块中心。
   - 它只用于判断是否接触木块，以及用逐帧 delta 推动木块。

2. block_center
   - 表示虚拟木块的真实逻辑中心。
   - 这是木块真实存在的位置。
   - 木块显示或隐藏都不影响 block_center 的存在。
   - contact / detach 判断必须基于当前真实 block_center，而不是 candidate。
   - candidate 只是预测位置，不能用于判断当前是否接触或脱离。

3. candidate_block_center
   - 表示“如果木块完全跟随本帧 pinch delta，它会到哪里”。
   - candidate = block_center + hand_delta。
   - candidate 不是木块真实位置。
   - 只有经过轨道约束检查后，candidate 或 clamp 后的位置才能成为新的 block_center。

核心运动规则：

- 木块不吸附到 pinch_center。
- 木块不强制维持 grab_offset。
- 木块只使用 pinch_center 的逐帧 delta 来移动。
- 每一帧：
  hand_delta = current_pinch_center - previous_pinch_center
  candidate_block_center = block_center + hand_delta

如果 candidate 在轨道区域内：
- block_center 更新到 candidate。
- 木块状态为正常移动。

如果 candidate 不在轨道区域内：
- 不要简单让 block_center 不动。
- 应该沿 current block_center -> candidate 这条线段，找到最后一个仍然位于轨道区域内的点。
- block_center 更新到这个最后合法点。
- 状态为被轨道阻挡。
- 需要输出 blocked_surface、blocked_amount、blocked_vector 等反馈信息。

轨道规则：

- 轨道是多个 axis-aligned 3D boxes 的并集。
- 判断一个点是否合法时，只要它在任意一个 box 内，就算在轨道内。
- clamp_segment_to_track 必须基于整个 track union，而不是只基于单个 active segment。
- 轨道边界附近需要 epsilon / feedback threshold，避免抖动。

木块显示规则：

- pinch_center 在木块 box 外：木块显示。
- pinch_center 第一次进入木块 box：木块立刻隐藏。
- 接触但 pinch distance 不足：木块仍然隐藏。
- pinch valid 且可移动：木块隐藏。
- pinch valid 但被轨道挡住：木块隐藏。
- 只有 pinch_center 离开木块 box，也就是真正 detach 后，木块才重新显示。

重要：木块隐藏不代表木块不存在。
隐藏时仍然要维护 block_center、block_size、contact box 和轨道约束。隐藏指的是在生成的画面中不能够看见物块，也不能看见滑轨。显示指的是在生成的画面中可以看见物块。

状态语义说明：

ContactState

1. OUTSIDE_BLOCK
   含义：
   - pinch_center 不在当前真实 block_center 对应的木块 box 内。
   - 手与木块分离。
   - 木块应该显示。
   - 此状态下不能推动木块。

2. INSIDE_BLOCK
   含义：
   - pinch_center 在当前真实 block_center 对应的木块 box 内。
   - 手与木块发生接触。
   - 木块应该隐藏。
   - 是否能推动木块还要看 pinch_state 和 track constraint。

PinchState

1. PINCH_VALID
   含义：
   - pinch_distance 足够小，认为受试者夹得足够紧。
   - 只有在 ContactState = INSIDE_BLOCK 且 PinchState = PINCH_VALID 时，木块才可能被推动。

2. PINCH_INSUFFICIENT
   含义：
   - pinch_distance 太大，受试者虽然接触了木块，但夹持力不足。
   - 木块隐藏。
   - 木块不移动。
   - 不计 release。
   - previous_pinch_center 仍然要更新，避免重新捏紧时木块跳变。

3. PINCH_UNKNOWN
   含义：
   - pinch 数据无效或缺失。
   - 木块不移动。
   - 不要触发 release 或 detach，除非明确检测到 pinch_center 离开 box。

BlockMotionState

1. FREE_VISIBLE
   含义：
   - pinch_center 在木块 box 外。
   - 木块显示。
   - 木块不受手控制。

2. CONTACT_HIDDEN
   含义：
   - pinch_center 刚进入木块 box。
   - 木块隐藏。
   - 这一帧只建立 reference，不移动木块。
   - 之后根据 pinch_state 决定是否能移动。

3. GRABBED_MOVING
   含义：
   - pinch_center 在木块 box 内。
   - pinch distance 有效。
   - hand_delta 未超过最大值。
   - candidate_block_center 在轨道区域内。
   - block_center 更新到 candidate。

4. GRABBED_BLOCKED
   含义：
   - pinch_center 在木块 box 内。
   - pinch distance 有效。
   - candidate_block_center 超出轨道区域。
   - 木块沿 block_center -> candidate 的线段移动到最后一个合法点。
   - 木块不能继续沿非法方向移动。
   - 需要输出 blocked_surface、blocked_amount、blocked_vector。
   - 木块仍然隐藏。
   - 这不是 detach。

5. GRABBED_PINCH_INSUFFICIENT
   含义：
   - pinch_center 仍在木块 box 内。
   - 但 pinch_distance 太大。
   - 木块隐藏。
   - 木块不移动。
   - 不计 release。
   - 如果之后 pinch_distance 重新变小，直接恢复控制，不重新显示，也不重新抓取。

6. STOPPED_BY_PINCH
   含义：
   - 这是给反馈系统使用的运动原因状态。
   - 表示木块没动的原因是 pinch distance 不足。
   - 触觉反馈可以据此提示“接触到了但夹不住”。

7. STOPPED_BY_TRACK
   含义：
   - 这是给反馈系统使用的运动原因状态。
   - 表示木块没动或只能移动到边界的原因是轨道限制。
   - 需要配合 blocked_surface / blocked_amount 使用。

8. STOPPED_BY_LARGE_DELTA
   含义：
   - 本帧 hand_delta 超过 max_hand_delta_per_frame。
   - 认为该帧可能是掉帧、追踪跳变或异常快速运动。
   - 木块不移动。
   - previous_pinch_center 仍然更新，避免下一帧累计巨大 delta。

DetachState

detach 的核心条件不是 pinch_distance 变大，而是 pinch_center 离开当前真实 block box。

pinch_distance 变大但 pinch_center 仍在 box 内：
- 不算 detach。
- 不计 release。
- 进入 GRABBED_PINCH_INSUFFICIENT / STOPPED_BY_PINCH。

只有 inside_block -> outside_block 的瞬间，才触发 detach。
detach 计数必须边缘触发，不能手在 box 外时每帧重复计数。

1. ACTIVE_RELEASE
   含义：
   - 离开 box 之前，主要状态是 GRABBED_PINCH_INSUFFICIENT 或 STOPPED_BY_PINCH。
   - 解释为受试者夹持力不足后离开木块。
   - 计入 active_release_count。
   - 也计入 total_detach_count。
   - 木块显示。

2. FORCED_DETACH
   含义：
   - 离开 box 之前，主要状态是 GRABBED_BLOCKED 或 STOPPED_BY_TRACK。
   - 解释为木块被轨道挡住，手继续移动，导致手从木块范围内脱离。
   - 计入 forced_detach_count。
   - 也计入 total_detach_count。
   - 木块显示。

3. UNEXPECTED_DETACH
   含义：
   - pinch valid，且之前没有明确 blocked 或 pinch insufficient，但 pinch_center 离开了 block box。
   - 这可能由快速移动、掉帧、追踪跳变或其他边界情况导致。
   - 用作鲁棒性兜底状态。
   - 计入 unexpected_detach_count。
   - 也计入 total_detach_count。
   - 木块显示。

4. NONE
   含义：
   - 本帧没有发生 detach。

每帧更新顺序必须遵守：

1. 读取当前输入：
   - pinch_center_task
   - pinch_distance
   - tracker_valid

2. 如果 tracker_valid = False：
   - 不更新 block_center。
   - 输出 tracking invalid / stopped 状态。
   - 不触发 detach，除非之后恢复时能明确判断。

3. 使用当前真实 block_center 判断 pinch_center 是否在木块 box 内。
   注意：这里不能使用 candidate_block_center。

4. 如果上一帧 inside_block=True，而这一帧 inside_block=False：
   - 触发 detach。
   - 根据上一帧主要状态分类为 ACTIVE_RELEASE / FORCED_DETACH / UNEXPECTED_DETACH。
   - 木块显示。
   - detach 计数只加一次。
   - 本帧不再推动木块。

5. 如果 inside_block=True：
   - 木块隐藏。
   - 计算 pinch_state。

6. 如果 pinch_state = PINCH_INSUFFICIENT：
   - 木块不移动。
   - 输出 GRABBED_PINCH_INSUFFICIENT / STOPPED_BY_PINCH。
   - 更新 previous_pinch_center。
   - 不计 release。

7. 如果 pinch_state = PINCH_VALID：
   - 计算 hand_delta。
   - 如果 hand_delta > max_hand_delta_per_frame：
       - 木块不移动。
       - 输出 STOPPED_BY_LARGE_DELTA。
       - 更新 previous_pinch_center。
   - 否则：
       - candidate = block_center + hand_delta。
       - 如果 candidate 在 track union 内：
           - block_center = candidate。
           - 输出 GRABBED_MOVING。
       - 如果 candidate 不在 track union 内：
           - block_center = clamp_segment_to_track(block_center, candidate, track_union)。
           - 输出 GRABBED_BLOCKED / STOPPED_BY_TRACK。
           - 输出 blocked 信息。

8. 每帧最后更新 previous_pinch_center，除非当前 pinch_center 无效。

9. contact/detach 判断永远只看当前真实 block_center 对应的木块 box。
   candidate 只是预测位置。


禁止事项：
- 不要把 candidate 当成真实 block_center。
- 不要用 candidate 判断 contact / detach。
- 不要在 pinch_distance 变大瞬间计 release。
- 不要让木块吸附到 pinch_center。
- 不要强制维持 grab_offset。
- 不要把所有逻辑写进一个巨大的 update 函数。
- 不要在核心引擎里接 SDK、socket、UI 或触觉硬件。
- 不要做真实物理碰撞、旋转碰撞、摩擦模型或刚体模拟。


新增 haptic_feedback.py。

核心引擎需要输出触觉反馈状态，但不要直接控制触觉硬件。

触觉反馈分为离散事件和持续状态。

离散事件：
1. CONTACT_ENTER
   - previous_contact_state = OUTSIDE_BLOCK
   - current_contact_state = INSIDE_BLOCK
   - 对应 pinch_center 进入木块 box
   - 边缘触发，只发一次

2. CONTACT_EXIT
   - previous_contact_state = INSIDE_BLOCK
   - current_contact_state = OUTSIDE_BLOCK
   - 对应 pinch_center 离开木块 box
   - 边缘触发，只发一次
   - 需要附带 detach reason:
     ACTIVE_RELEASE / FORCED_DETACH / UNEXPECTED_DETACH

持续状态：
1. SLIP
   - contact_state = INSIDE_BLOCK
   - pinch_center 在 box 内移动
   - 但 pinch_distance 不足以驱动木块，或者木块被轨道挡住导致手和木块发生相对运动
   - SLIP 不是“pinch distance 不足”本身。
   - SLIP = pinch_center 在 block box 内 + pinch_center 相对上一帧发生移动 + 当前不能有效驱动木块。
   - Haptic SLIP definition:
SLIP is a motion-dependent haptic state. Do not trigger SLIP only because pinch_distance is insufficient. SLIP should be active only when pinch_center is inside the current block box, hand_delta magnitude is larger than slip_motion_threshold, and the block cannot be normally driven because pinch is insufficient or the track blocks the block motion.

If hand_delta magnitude is below slip_motion_threshold, do not emit SLIP. Instead, keep the corresponding static state such as PINCH_INSUFFICIENT or GRABBED_BLOCKED.
   - 输出 slip_reason:
     PINCH_INSUFFICIENT
     TRACK_BLOCKED
     BOTH

2. BLOCKED_FORCE
   - block_motion_state = GRABBED_BLOCKED 或 STOPPED_BY_TRACK
   - pinch_center 仍在 box 内
   - 输出：
     force_vector_task = block_center_task - pinch_center_task
     force_magnitude = norm(force_vector_task)
     primary_blocked_surface
     primary_blocked_amount
   - 第一版只需要计算并输出这些值，不需要实现具体触觉映射。

要求：
- 触觉反馈状态只能由核心状态推导。
- haptic 模块不能反过来修改 block_center、pinch_state、track_state。
- 触觉事件必须记录到 events.csv。
- 持续触觉状态可以记录到 frames.csv。

Tracking recovery rule:
When tracker_valid changes from False to True, the first valid frame is a recovery frame. On this frame, do not move the block, do not emit CONTACT_ENTER or CONTACT_EXIT, do not trigger detach/release, and only reset previous_pinch_center to current_pinch_center. Normal contact edge events resume from the next frame.

Slip reason rule:
In the first implementation, SLIP can have reason PINCH_INSUFFICIENT or TRACK_BLOCKED. BOTH may remain in the enum as a reserved future value, but the current state machine should not emit BOTH because pinch-insufficient frames do not proceed to track-blocked checks.