你现在要修改 exp2 的 stage-5 分支。

目标：
新增 Stage 5B-0：live raw stream bridge / realtime input smoke test。

背景：
当前通讯仓库 manus_vive_com 的 exp2-test 分支已经有：
- C++ SDK client 通过 TCP 发送 combined JSON
- manus_data_receiver.py 作为 Python TCP server 接收 newline-delimited JSON
- capture_raw_jsonl.py 可以把收到的 frame_data 原样保存为 raw JSONL

当前 exp2 已经有：
- raw parser
- DeviceFrame
- ManusViveExperimentAdapter
- SessionRecorder
- calibration_io / formal_table_lines calibration
- map_config / analyzer

本阶段目标：
让 exp2 可以直接接收 manus_vive_com 发来的 newline JSON stream，实时解析、转换、记录健康指标。
本阶段不是正式实验，不启动 TrialController，不做 haptic，不写 GUI。

不要修改：
- BlockController
- TrialController
- TaskCoordinateSystem
- ManusViveExperimentAdapter 核心逻辑
- raw parser 语义
- haptic 语义
- manus_vive_com 仓库代码

新增模块：
live_raw_stream.py

功能：
实现一个 exp2 内部的 newline JSON TCP server / source，兼容 manus_vive_com 当前发送协议。

建议结构：
1. LiveRawFrame
字段：
- frame_index
- raw_frame
- receive_time_monotonic
- receive_wall_time
- byte_length

2. LiveRawStreamServer
参数：
- host 默认 127.0.0.1
- port 默认 8888
- max_queue_size 默认 300
- socket_timeout 可选
- stop_event 可选

行为：
- bind host/port
- 等待一个客户端连接
- 持续接收 bytes
- 以 newline 分割 JSON
- json.loads
- 给每帧附加 receive_time_monotonic
- 推入线程安全 queue
- 如果 queue 满，丢弃最旧帧或最新帧，但必须统计 dropped_frame_count
- 不改变 raw JSON 字段
- 提供 iter_frames() 或 get_frame(timeout) API

注意：
不要 import manus_vive_com。
只实现兼容协议：newline-delimited JSON over TCP。

新增脚本：
run_live_raw_preview.py

输入参数：
- --host 默认 127.0.0.1
- --port 默认 8888
- --duration-seconds 可选
- --max-frames 可选
- --thumb-node 默认 4
- --index-node 默认 9
- --tracker-index 默认 0
- --skeleton-index 默认 0
- --timestamp-scale 默认 0.001
- --out-dir 默认 data/live_raw_preview
- --write-session 可选
- --session-dir 可选
- --print-every 默认 30
- --save-raw-jsonl 可选，默认 true

实时流程：
每收到一帧：
1. 原样保存 raw frame 到 raw_frames.jsonl，如果 save_raw_jsonl=true
2. parse_raw_manus_vive_frame
3. ManusViveExperimentAdapter 转 ExperimentInputSample
4. 不启动 TrialController
5. 记录 live metrics

输出：
out_dir/
- raw_frames.jsonl
- live_metrics.csv
- live_summary.json

live_metrics.csv 字段至少包括：
- frame_index
- raw_timestamp
- receive_time_monotonic
- receive_wall_time
- parse_ok
- adapter_ok
- tracker_valid
- hand_valid
- pinch_valid
- pinch_distance
- skeleton_count
- tracker_count
- sync_delta_ms
- inter_receive_interval_ms
- processing_latency_ms
- queue_size
- dropped_frame_count
- error_message

live_summary.json 至少包括：
- total_received_frames
- total_processed_frames
- parse_error_count
- adapter_error_count
- tracker_valid_frame_count
- hand_valid_frame_count
- pinch_valid_frame_count
- dropped_frame_count
- mean_receive_fps
- mean_processing_latency_ms
- max_processing_latency_ms
- mean_sync_delta_ms
- max_sync_delta_ms
- warnings

如果 --write-session：
- 创建 session 目录
- mode = "live_raw_preview"
- is_formal_calibration = false
- is_live_trial = false
- 不需要 TrialController 输出
- 可以记录 raw_frames.jsonl / device_frames.jsonl
- processed_frames.csv 如果当前 SessionRecorder 需要 FrameOutput 才能写，可以先不强行写完整 processed frame；或者写 minimal processed frame，但不要制造 TrialController 状态

测试：
新增 tests/test_live_raw_stream.py
- 用本地 socket client 发送几行 JSON
- LiveRawStreamServer 能接收
- newline 分包/粘包能处理
- bad JSON 不中断整个 server，只计 parse error
- queue 满时 dropped_frame_count 增加

新增 tests/test_run_live_raw_preview.py
- 使用 fake socket client 或 fake source 跑 run_live_raw_preview
- 生成 raw_frames.jsonl
- 生成 live_metrics.csv
- 生成 live_summary.json
- 不依赖 manus_vive_com
- 不需要真实设备

README：
增加 Stage 5B-0 说明：
- 这是 live raw stream smoke test
- 用于验证 manus_vive_com stream 能否被 exp2 实时消费
- 不做 calibration
- 不做 TrialController
- 不做 haptic
- 不做 GUI

示例：
1. 启动 exp2 receiver:
python run_live_raw_preview.py --host 127.0.0.1 --port 8888 --out-dir data/live_raw_preview/test

2. 启动 manus_vive_com C++ client，让它连接 127.0.0.1:8888 发送 combined JSON

完成后运行 pytest -q，并报告：
1. 新增文件
2. 修改文件
3. 新增测试
4. pytest 是否通过
5. 如何用 manus_vive_com 的实时流测试 exp2 live raw preview

认可这些默认选择，可以开始实现。

请按以下语义执行：

1. queue 满时默认丢弃最旧帧，保留最新实时状态，并统计 dropped_frame_count。live_summary.json 中记录 queue_drop_policy = "drop_oldest_when_full"。

2. client 断开后第一版直接结束 preview，不做自动重连。live_summary.json 中记录 stop_reason = "client_disconnected" / "max_frames" / "duration_reached" / "keyboard_interrupt" 等。

3. bad JSON 行不写 live_metrics.csv 的逐帧行，只计入 parse_error_count / bad_json_line_count。可以记录 last_parse_error_message 和截断后的 bad_json preview，但不要写整行大 payload。

4. --save-raw-jsonl 默认 true，并提供 --no-save-raw-jsonl。

5. --write-session 时只写 session_meta.json、raw_frames.jsonl、device_frames.jsonl、trial_summary.json / live_summary.json。不要伪造 TrialController 的 processed_frames/events/haptic。可以创建空 CSV，但必须在 session_meta.json 中写明：
   - mode = live_raw_preview
   - is_live_trial = false
   - trial_controller_started = false
   - processed_frames_are_trial_outputs = false

6. 继续不接 haptic hardware，不启动 TrialController，不写 GUI，不修改核心 parser/adapter/controller 逻辑。