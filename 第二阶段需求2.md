这 6 个默认假设基本确认，但第 4 和第 6 点稍微调整：

1. start_trial() 第一版直接进入 RUNNING。
   start_trial(time) 同时记录 trial_started 和 prompt_on，prompt_on_time = time。
   暂时不需要单独 PROMPT 帧。

2. subject_end=True 的那一帧，先调用 BlockController.update()，记录最后一帧 frame_output / haptic state / block events，然后再切到 ENDED_BY_SUBJECT 并记录 subject_end event。

3. timeout 和 too_many_detaches 在本帧 BlockController.update() 之后检查。
   同一帧如果 subject_end=True，第一版 subject_end 优先于 timeout / detach failure。

4. 多 trial 重置建议第一版使用 block_controller_factory。
   TrialController 在 start_trial() 时创建新的 BlockController 实例，避免旧 trial 的 previous_pinch_center、inside_block、pinch state、tracking recovery flag、detach counts 污染新 trial。
   暂时不强制给 BlockController 增加 reset()；如果后面需要复用实例，再单独加显式 reset(initial_block_center)。

5. tracking_invalid 和 tracking_recovered 都按边缘触发：
   - valid -> invalid 记录 tracking_invalid 一次
   - invalid -> valid 记录 tracking_recovered 一次
   invalid 期间 frames.csv 每帧记录 tracker_valid=False，但 events.csv 不重复刷 tracking_invalid。

6. 事件记录规则：
   - pinch_valid / pinch_insufficient 按状态切换记。
   - contact_enter / contact_exit 按边缘记。
   - slip 用 slip_start / slip_end 记，frames.csv 每帧记录 slip_active。
   - blocked_force 用 blocked_force_start / blocked_force_end 记，frames.csv 每帧记录 blocked_force_active。
   - block_moved 不建议每帧写 events.csv，frames.csv 通过 block_center 和 block_motion_state 记录即可。
   - block_blocked 建议记 block_blocked_start / block_blocked_end，而不是每帧刷。
   - large_delta 可以每次发生都记 event，因为它通常是异常帧。